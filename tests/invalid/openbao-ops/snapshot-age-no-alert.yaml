# A watch that tells nobody is a log line.
snapshotAge:
  enabled: true
  stores:
    - name: primary
      prefix: raft/
      maxAgeSeconds: 43200
      s3: {enabled: true, bucket: example-openbao-backups, region: eu-example-1}
